<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card text-center">
                <div class="card-body">
                    <img src="/img/user-icon.png" height="100px" width="100px">
                    <h3>Welcome {{user.fullname}}</h3>
                    <h4>{{user.username}}</h4>
                    <a href="/profile/edit/{{user.id}}" class="btn btn-secondary">Editar</a>
                </div>
            </div>
        </div>
    </div>
</div>